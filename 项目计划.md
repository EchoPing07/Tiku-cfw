# Tiku-cfw 项目计划

> 基于 Cloudflare Worker 的 AI 题库服务，兼容 OCS 网课助手 AnswererWrapper 规范。

---

## 一、最终确认决策

| # | 决策项 | 确认结果 |
|---|--------|----------|
| 1 | AI Provider | OpenAI 兼容接口（含第三方中转站），统一接口 |
| 2 | AI 模型分类 | 文本模型 + 视觉模型两大类，纯文走文本，带图走视觉 |
| 3 | AI 渠道 | 多渠道配置，每渠道多 Key，权重优先，同权重随机 |
| 4 | Key 轮询策略 | 最少使用优先（least_used） |
| 5 | 失败处理 | 失败计数达阈值（默认3，可配置）自动禁用 Key，降级到下一个 Key/渠道 |
| 6 | 缓存匹配 | 仅精确匹配（归一化后 MD5 完全相等） |
| 7 | 缓存过期 | 永不过期，只能手动删除 |
| 8 | 答案审核 | AI 答完直接入缓存，标记来源为 AI，面板可修改/删除 |
| 9 | 管理员 | 单管理员，只需密码，不需要用户名 |
| 10 | 管理员密码 | 源码默认 `password`，可通过环境变量 `ADMIN_PASSWORD` 调整 |
| 11 | API Key | 明文可查看、支持多 Key、可备注、快速复制完整 OCS 配置 |
| 12 | 限流 | 不需要限流 |
| 13 | 日志保留 | 面板可配置保留天数 |
| 14 | 导入导出 | JSON + CSV 都支持 |
| 15 | UI 主题 | 跟随系统 / 可手动切换明暗 |
| 16 | Token 统计 | 仅统计调用次数，不统计 token 用量 |
| 17 | 图片支持 | V1 就支持，OCS 用自定义 handler 提取图片 URL 传给 API |
| 18 | 题目类型 | 可选，不传时 AI 自行判断，需完善提示词 |

---

## 二、项目概述

### 2.1 定位

一个部署在 Cloudflare Worker 上的 AI 题库服务，提供：

- **搜题接口**：兼容 OCS `AnswererWrapper` 规范，OCS 脚本可直接对接
- **AI 作答**：未命中缓存时调用 AI 大模型生成答案，支持多渠道负载均衡与故障降级
- **题目缓存**：D1 数据库缓存题目-答案对，避免重复调用 AI 节约成本
- **Web 管理面板**：密码登录、仪表盘统计、题库管理、API 密钥管理、AI 渠道配置

### 2.2 技术栈

| 层级 | 技术 | 说明 |
|------|------|------|
| 运行时 | Cloudflare Workers | Serverless 边缘计算 |
| 数据库 | Cloudflare D1 | SQLite，存储题目缓存、配置、日志 |
| AI | OpenAI 兼容接口 | 统一接口，支持任意 OpenAI 兼容的 API |
| 前端 | 原生 HTML + Tailwind CDN | 单 HTML 文件 SPA，Worker 直接返回 |
| 语言 | TypeScript | 全量类型安全 |
| 构建 | Wrangler | CF 官方 CLI |

### 2.3 核心流程

```
OCS 脚本请求 POST /api/search
    │
    ├── 1. API Key 鉴权（明文比对）
    │
    ├── 2. 题目归一化 → 计算 MD5 hash
    │
    ├── 3. 查 D1 缓存（精确匹配 question_hash）
    │       ├── 命中 → hit_count++ → 返回缓存答案
    │       └── 未命中 ↓
    │
    ├── 4. AI 多渠道调度
    │       ├── 判断是否带图片 → 选 text / vision 渠道
    │       ├── 按权重分组，高权重优先，同权重随机
    │       ├── 渠道内选 use_count 最少的 Key
    │       ├── 调用 AI → 成功则 use_count++，fail_count 清零
    │       └── 失败则 fail_count++，达阈值禁用 Key，降级下一个
    │
    ├── 5. 答案后处理（格式化、分隔符规范）
    │
    ├── 6. 写入 D1 缓存 + 写入搜索日志
    │
    └── 7. 返回 OCS 兼容格式 {code, question, answer, msg}
```

---

## 三、目录结构

```
Tiku-cfw/
├── src/
│   ├── index.ts                  # Worker 入口，路由分发
│   │
│   ├── api/                      # API 路由
│   │   ├── search.ts             # 搜题接口（OCS 兼容）
│   │   └── health.ts             # 健康检查
│   │
│   ├── admin/                    # 管理面板 API
│   │   ├── auth.ts               # 登录/登出/刷新 token
│   │   ├── dashboard.ts          # 仪表盘统计
│   │   ├── questions.ts          # 题目管理（增删改查/导入/导出）
│   │   ├── keys.ts               # API 密钥管理
│   │   ├── channels.ts           # AI 渠道管理
│   │   ├── settings.ts           # 系统设置
│   │   └── logs.ts               # 日志查询/清理
│   │
│   ├── ai/                       # AI 集成层
│   │   ├── types.ts              # 类型定义
│   │   ├── dispatcher.ts         # 多渠道调度器（权重/降级/轮询）
│   │   ├── openai-client.ts      # OpenAI 兼容请求客户端
│   │   ├── prompt.ts             # Prompt 模板构建
│   │   └── answer-parser.ts      # AI 响应解析与格式化
│   │
│   ├── cache/                    # 缓存层
│   │   └── normalize.ts          # 题目归一化 + hash
│   │
│   ├── auth/                     # 认证层
│   │   ├── jwt.ts                # JWT 签发/验证
│   │   └── middleware.ts         # 认证中间件
│   │
│   ├── web/                      # 前端面板
│   │   └── index.html            # SPA 单页应用（含明暗主题切换）
│   │
│   ├── types/
│   │   └── env.ts                # Worker 环境变量绑定类型
│   │
│   └── utils/                    # 工具函数
│       ├── response.ts           # 统一响应封装
│       ├── cors.ts               # CORS 处理
│       └── id.ts                 # ID 生成（crypto.randomUUID）
│
├── migrations/                   # D1 迁移文件
│   ├── 0001_init.sql             # 建表
│   └── 0002_seed.sql             # 初始数据
├── wrangler.toml
├── package.json
├── tsconfig.json
├── .dev.vars
├── .gitignore
└── README.md
```

---

## 四、数据库设计 (D1)

### 4.1 表结构

```sql
-- ============ 题目缓存表 ============
CREATE TABLE IF NOT EXISTS questions (
    id            TEXT PRIMARY KEY,
    question      TEXT NOT NULL,             -- 原始题目
    question_norm TEXT NOT NULL,             -- 归一化题目
    question_hash TEXT NOT NULL UNIQUE,      -- MD5(归一化题目)
    answer        TEXT NOT NULL,             -- 答案
    type          TEXT,                      -- single/multiple/judgement/completion
    options       TEXT,                      -- 选项（JSON 数组）
    source        TEXT DEFAULT 'ai',         -- ai/manual/import
    ai_model      TEXT,                      -- 生成该答案的模型
    has_images    INTEGER DEFAULT 0,         -- 是否含图片
    hit_count     INTEGER DEFAULT 0,         -- 命中次数
    created_at    TEXT DEFAULT (datetime('now')),
    updated_at    TEXT DEFAULT (datetime('now'))
);

CREATE INDEX IF NOT EXISTS idx_question_hash ON questions(question_hash);
CREATE INDEX IF NOT EXISTS idx_type ON questions(type);
CREATE INDEX IF NOT EXISTS idx_created_at ON questions(created_at);

-- ============ API 密钥表（明文存储，可查看）============
CREATE TABLE IF NOT EXISTS api_keys (
    id          TEXT PRIMARY KEY,
    key         TEXT NOT NULL UNIQUE,         -- 明文 API Key，如 tk_xxxxxxxx
    name        TEXT NOT NULL DEFAULT '',     -- 备注名
    enabled     INTEGER DEFAULT 1,
    expires_at  TEXT,                         -- 过期时间（NULL=永不过期）
    use_count   INTEGER DEFAULT 0,            -- 使用次数
    last_used   TEXT,                         -- 最后使用时间
    created_at  TEXT DEFAULT (datetime('now'))
);

-- ============ AI 渠道表 ============
CREATE TABLE IF NOT EXISTS ai_channels (
    id          TEXT PRIMARY KEY,
    name        TEXT NOT NULL,                -- 渠道名称
    type        TEXT NOT NULL,                -- 'text' | 'vision'
    base_url    TEXT NOT NULL,                -- API 地址，如 https://api.openai.com/v1
    model       TEXT NOT NULL,                -- 模型名称，如 gpt-4o-mini
    weight      INTEGER DEFAULT 1,            -- 权重，越高越优先
    temperature REAL DEFAULT 0.3,
    max_tokens  INTEGER DEFAULT 2000,
    enabled     INTEGER DEFAULT 1,
    created_at  TEXT DEFAULT (datetime('now')),
    updated_at  TEXT DEFAULT (datetime('now'))
);

-- ============ AI 渠道密钥表（明文存储，可查看）============
CREATE TABLE IF NOT EXISTS ai_channel_keys (
    id          TEXT PRIMARY KEY,
    channel_id  TEXT NOT NULL,                -- 关联渠道
    api_key     TEXT NOT NULL,                -- 明文 API Key
    name        TEXT DEFAULT '',              -- 备注名
    enabled     INTEGER DEFAULT 1,
    use_count   INTEGER DEFAULT 0,            -- 使用次数（最少使用优先）
    fail_count  INTEGER DEFAULT 0,            -- 连续失败次数
    last_used   TEXT,
    created_at  TEXT DEFAULT (datetime('now')),
    FOREIGN KEY (channel_id) REFERENCES ai_channels(id) ON DELETE CASCADE
);

CREATE INDEX IF NOT EXISTS idx_channel_keys_channel ON ai_channel_keys(channel_id);

-- ============ 系统设置表 ============
CREATE TABLE IF NOT EXISTS settings (
    key         TEXT PRIMARY KEY,
    value       TEXT NOT NULL,
    description TEXT,
    updated_at  TEXT DEFAULT (datetime('now'))
);

-- ============ 搜索日志表 ============
CREATE TABLE IF NOT EXISTS search_logs (
    id            TEXT PRIMARY KEY,
    question      TEXT NOT NULL,
    question_hash TEXT NOT NULL,
    found         INTEGER DEFAULT 0,           -- 是否命中
    from_cache    INTEGER DEFAULT 0,           -- 是否来自缓存
    answer        TEXT,
    ai_channel    TEXT,                        -- 使用的渠道名
    ai_model      TEXT,                        -- 使用的模型
    duration_ms   INTEGER DEFAULT 0,           -- 耗时（毫秒）
    api_key_id    TEXT,                        -- 使用的 API Key ID
    error         TEXT,
    created_at    TEXT DEFAULT (datetime('now'))
);

CREATE INDEX IF NOT EXISTS idx_logs_created ON search_logs(created_at);
CREATE INDEX IF NOT EXISTS idx_logs_found ON search_logs(found);
```

### 4.2 初始数据

```sql
INSERT OR IGNORE INTO settings (key, value, description) VALUES
    ('site_name',           'Tiku-cfw',  '站点名称'),
    ('log_retention_days',  '30',        '日志保留天数（0=永久）'),
    ('ai_timeout',          '30',        'AI 请求超时秒数'),
    ('key_fail_threshold',  '3',         'Key 连续失败禁用阈值'),
    ('cors_origins',        '*',         'CORS 允许的域名'),
    ('system_prompt',       '',          '自定义系统提示词（空=用默认）');
```

---

## 五、AI 多渠道调度设计（核心）

### 5.1 概念模型

```
题目请求
  │
  ├─ 有图片？──→ vision 渠道池
  │                 ├── 渠道A (weight=10)
  │                 │    ├── key1 (use_count=5)
  │                 │    └── key2 (use_count=3) ← 选这个
  │                 ├── 渠道B (weight=5)  ← 同权随机备选
  │                 └── 渠道C (weight=1)  ← 最低优先级
  │
  └─ 无图片 ──→ text 渠道池
                    ├── 渠道D (weight=10)
                    │    └── key3 (use_count=1) ← 选这个
                    └── 渠道E (weight=5)
                         └── key4 (use_count=0)
```

### 5.2 调度算法

```typescript
async function dispatchAI(params: {
  type: 'text' | 'vision';
  messages: ChatMessage[];
  env: Env;
  db: D1Database;
}): Promise<{ content: string; channel: string; model: string }> {

  // 1. 查询该类型下所有启用的渠道，按 weight DESC 排序
  const channels = await db.prepare(`
    SELECT * FROM ai_channels
    WHERE type = ? AND enabled = 1
    ORDER BY weight DESC
  `).bind(params.type).all();

  // 2. 按权重分组
  const groups = groupByWeight(channels);

  // 3. 从最高权重组开始尝试
  for (const group of groups) {
    // 同权重组内随机打乱顺序
    const shuffled = shuffle(group);

    for (const channel of shuffled) {
      // 4. 查询该渠道下启用的 keys，按 use_count ASC 排序（最少使用优先）
      const keys = await db.prepare(`
        SELECT * FROM ai_channel_keys
        WHERE channel_id = ? AND enabled = 1
        ORDER BY use_count ASC
      `).bind(channel.id).all();

      if (keys.length === 0) continue; // 此渠道无可用 key，跳过

      // 5. 依次尝试每个 key
      for (const key of keys) {
        try {
          // 6. 调用 AI API
          const result = await callOpenAI({
            baseUrl: channel.base_url,
            apiKey: key.api_key,
            model: channel.model,
            messages: params.messages,
            temperature: channel.temperature,
            maxTokens: channel.max_tokens,
            timeout: settings.ai_timeout,
          });

          // 7. 成功：use_count++，fail_count=0，last_used=now
          await db.prepare(`
            UPDATE ai_channel_keys
            SET use_count = use_count + 1, fail_count = 0, last_used = datetime('now')
            WHERE id = ?
          `).bind(key.id).run();

          return { content: result, channel: channel.name, model: channel.model };

        } catch (error) {
          // 8. 失败：fail_count++
          const newFailCount = key.fail_count + 1;
          const threshold = settings.key_fail_threshold; // 默认 3

          if (newFailCount >= threshold) {
            // 达到阈值，禁用此 key
            await db.prepare(`
              UPDATE ai_channel_keys
              SET fail_count = ?, enabled = 0
              WHERE id = ?
            `).bind(newFailCount, key.id).run();
          } else {
            // 未达阈值，仅增加失败计数
            await db.prepare(`
              UPDATE ai_channel_keys SET fail_count = ? WHERE id = ?
            `).bind(newFailCount, key.id).run();
          }

          // 继续尝试下一个 key
          continue;
        }
      }
      // 此渠道所有 key 都失败，尝试下一个渠道
    }
    // 此权重组所有渠道都失败，尝试下一个权重组
  }

  // 所有渠道都失败
  throw new Error('所有 AI 渠道均不可用');
}
```

### 5.3 失败处理规则

| 错误类型 | 处理方式 |
|----------|----------|
| 超时 | fail_count++，尝试同渠道下一个 Key |
| 401/403（认证失败） | fail_count++，尝试下一个 Key |
| 429（限流） | fail_count++，尝试下一个 Key |
| 500/502/503（服务器错误） | fail_count++，尝试下一个 Key |
| 网络错误 | fail_count++，尝试下一个 Key |
| Key 被禁用（fail_count ≥ 阈值） | 跳过此 Key，尝试下一个 |
| 渠道所有 Key 都失败 | 跳过此渠道，尝试下一个同权重组渠道 |
| 同权重组所有渠道失败 | 降级到下一权重组 |
| 所有渠道失败 | 返回未找到（code: 0） |

### 5.4 Key 重置

- 管理员可在面板手动「重置失败计数」和「重新启用」被禁用的 Key
- 重置时 `fail_count = 0, enabled = 1`

---

## 六、Prompt 设计

### 6.1 系统提示词（默认，可自定义）

```
你是一个专业的题库答题助手。请根据提供的题目和选项，给出正确答案。

## 答题规则

1. 单选题：返回正确选项的内容或字母（如 A）
2. 多选题：多个答案用 # 分隔，如 A#B#C
3. 判断题：返回"正确"或"错误"
4. 填空题：多个空用 # 分隔，如 答案1#答案2#答案3
5. 只返回答案本身，不要任何解释、分析或前缀
6. 不要说"答案是"，直接返回答案内容
7. 如果题目类型未指定，请自行判断题型
8. 如果不确定，返回你最有把握的答案

## 格式要求

- 选择题答案可以是字母（A）或选项完整内容
- 多选答案必须用 # 分隔
- 判断题只能返回"正确"或"错误"两个词之一
- 填空题多空必须用 # 分隔
- 不要使用 markdown 格式
- 不要换行，答案必须是单行
```

### 6.2 用户消息构建

**纯文本题目：**
```
题目类型：单选题
题目：中国梦是什么？
选项：
A. 实现中华民族伟大复兴
B. 全面建成小康社会
C. 建设社会主义现代化强国
D. 实现共同富裕
```

**带图片题目（vision 模型）：**
```json
{
  "role": "user",
  "content": [
    {"type": "text", "text": "题目类型：单选题\n题目：下图所示的结构是什么？\n选项：\nA. DNA\nB. RNA\nC. 蛋白质\nD. 多糖"},
    {"type": "image_url", "image_url": {"url": "https://example.com/image.png"}}
  ]
}
```

### 6.3 答案后处理

```typescript
function parseAIAnswer(raw: string, type?: string): string {
  let answer = raw.trim();

  // 1. 去除 markdown 代码块
  answer = answer.replace(/```[\s\S]*?```/g, '').trim();

  // 2. 去除常见前缀
  answer = answer.replace(/^(答案[是为：:]*|正确答案[是为：:]*|解析[：:])\s*/gi, '').trim();

  // 3. 去除换行（答案必须是单行）
  answer = answer.replace(/\n/g, ' ').trim();

  // 4. 如果是选择题且答案以字母开头，规范化
  if ((type === 'single' || type === 'multiple' || !type) && /^[A-Z]/i.test(answer)) {
    const letters = answer.match(/[A-Z]/gi);
    if (letters && letters.length > 0) {
      // 如果答案只是字母组合（如 "AB" 或 "A, B, C"）
      if (answer.replace(/[,，、\s#]/g, '').match(/^[A-Z]+$/i)) {
        return letters.join('#').toUpperCase();
      }
    }
  }

  // 5. 判断题规范化
  if (type === 'judgement' || isJudgementAnswer(answer)) {
    if (/对|正确|是|true|√|T|yes/i.test(answer)) return '正确';
    if (/错|错误|否|false|×|F|no/i.test(answer)) return '错误';
  }

  return answer;
}
```

---

## 七、题目归一化

```typescript
function normalizeQuestion(question: string): string {
  return question
    .replace(/<[^>]+>/g, '')                    // HTML 标签
    .replace(/&[a-z]+;/gi, ' ')                 // HTML 实体→空格
    .replace(/【[^】]*】/g, '')                   // 【单选题】等标记
    .replace(/\(\d+分\)/g, '')                   // (5分) 分值标记
    .replace(/（\d+分）/g, '')                    // （5分）全角分值
    .replace(/[\s\u3000]+/g, '')                 // 所有空白含全角空格
    .replace(/[，。、；：！？""''（）【】《》]/g, '') // 中文标点
    .replace(/[,.!;:?()"'\[\]{}<>]/g, '')        // 英文标点
    .toLowerCase()
    .trim();
}

async function questionHash(normalized: string): Promise<string> {
  const data = new TextEncoder().encode(normalized);
  const hash = await crypto.subtle.digest('MD5', data);
  return [...new Uint8Array(hash)].map(b => b.toString(16).padStart(2, '0')).join('');
}
```

> **注意**：CF Worker 的 `crypto.subtle` 不支持 MD5，改用 SHA-256。

---

## 八、API 接口设计

### 8.1 搜题接口（OCS 兼容）

```
POST /api/search
```

**鉴权**：Header `Authorization: Bearer <api-key>` 或 Query `?key=<api-key>`

**请求体**：
```json
{
  "title": "中国梦是什么？",
  "type": "single",
  "options": "A. xxx\nB. xxx",
  "images": ["https://example.com/img1.png"]
}
```

| 字段 | 类型 | 必填 | 说明 |
|------|------|------|------|
| `title` | string | 是 | 题目标题 |
| `type` | string | 否 | 题型，不传时 AI 自行判断 |
| `options` | string | 否 | 题目选项 |
| `images` | string[] | 否 | 图片 URL 数组，有图走视觉模型 |

**响应**：
```json
{
  "code": 1,
  "question": "中国梦是什么？",
  "answer": "实现中华民族伟大复兴",
  "msg": "成功"
}
```

### 8.2 管理面板 API

| 方法 | 路径 | 说明 |
|------|------|------|
| POST | `/api/admin/login` | 登录（只需密码） |
| GET | `/api/admin/dashboard` | 仪表盘统计 |
| GET | `/api/admin/questions` | 题目列表（分页/搜索） |
| POST | `/api/admin/questions` | 添加题目 |
| PUT | `/api/admin/questions/:id` | 编辑题目 |
| DELETE | `/api/admin/questions/:id` | 删除题目 |
| POST | `/api/admin/questions/import` | 批量导入（JSON/CSV） |
| GET | `/api/admin/questions/export` | 导出（JSON/CSV） |
| GET | `/api/admin/keys` | API 密钥列表（含明文） |
| POST | `/api/admin/keys` | 创建密钥 |
| PUT | `/api/admin/keys/:id` | 编辑密钥 |
| DELETE | `/api/admin/keys/:id` | 删除密钥 |
| GET | `/api/admin/channels` | AI 渠道列表 |
| POST | `/api/admin/channels` | 创建渠道 |
| PUT | `/api/admin/channels/:id` | 编辑渠道 |
| DELETE | `/api/admin/channels/:id` | 删除渠道 |
| GET | `/api/admin/channels/:id/keys` | 渠道下 Key 列表 |
| POST | `/api/admin/channels/:id/keys` | 添加 Key |
| PUT | `/api/admin/channel-keys/:id` | 编辑 Key |
| DELETE | `/api/admin/channel-keys/:id` | 删除 Key |
| POST | `/api/admin/channel-keys/:id/reset` | 重置失败计数/重新启用 |
| GET | `/api/admin/settings` | 系统设置 |
| PUT | `/api/admin/settings` | 更新设置 |
| GET | `/api/admin/logs` | 搜索日志（分页） |
| DELETE | `/api/admin/logs` | 清空/按天数清理日志 |
| GET | `/api/health` | 健康检查 |

---

## 九、Web 管理面板

### 9.1 页面规划

| 页面 | 功能 |
|------|------|
| 登录 | 密码登录（无用户名） |
| 仪表盘 | 题目总数、缓存命中率、AI 调用次数、最近日志 |
| 题库管理 | 列表/搜索/编辑/删除/导入(JSON+CSV)/导出(JSON+CSV) |
| API 密钥 | 列表(含明文)/创建/编辑/删除/一键复制OCS配置 |
| AI 渠道 | 渠道列表/创建/编辑/删除；渠道下Key管理/重置 |
| 系统设置 | 日志保留天数、超时、失败阈值、CORS、系统提示词 |
| 搜索日志 | 列表/筛选/清理 |

### 9.2 主题切换

- 使用 Tailwind `dark:` 类 + `class` 策略
- `localStorage` 记忆用户选择
- 首次访问跟随 `prefers-color-scheme`
- 顶栏切换按钮：☀️ / 🌙 / 🖥️（亮/暗/自动）

### 9.3 API 密钥页特色

- 列表直接显示完整密钥（明文）
- 每个密钥有备注名
- 「复制 OCS 配置」按钮：生成包含该密钥的完整 AnswererWrapper JSON
- 显示使用次数和最后使用时间

---

## 十、认证设计

### 10.1 管理员认证

- **无用户名**，只输入密码
- 密码来源：环境变量 `ADMIN_PASSWORD`，默认值 `password`
- 登录成功返回 JWT（有效期 24h）
- JWT 载荷：`{ exp }`（不含用户名，因为只有一个管理员）
- 密码不存数据库，直接比对环境变量

### 10.2 API Key 认证

- 格式：`tk_<32位随机hex>`
- **明文存储**，面板可查看
- 支持多个 Key
- 每个 Key 可设置备注名
- 可设置过期时间
- 鉴权时直接明文比对

---

## 十一、环境变量

```toml
# wrangler.toml
[vars]
ADMIN_PASSWORD = "password"       # 管理员密码，默认 password
JWT_SECRET = "your-jwt-secret"    # JWT 签名密钥
```

```typescript
interface Env {
  DB: D1Database;
  ADMIN_PASSWORD: string;   // 管理员密码，默认 "password"
  JWT_SECRET: string;       // JWT 密钥
}
```

---

## 十二、开发阶段

### Phase 1: 核心骨架

- Worker 入口 + 路由
- D1 建表 + 初始数据
- 题目归一化 + 精确匹配缓存
- AI 单渠道调用（OpenAI 兼容）
- `/api/search` 搜题接口
- API Key 鉴权
- 基础 Prompt + 答案解析

### Phase 2: 管理面板

- 密码登录 + JWT
- 仪表盘
- 题库管理（CRUD + 导入导出）
- API 密钥管理（含 OCS 配置复制）
- 搜索日志
- 明暗主题切换

### Phase 3: AI 多渠道

- 渠道管理（text/vision）
- 渠道 Key 管理
- 权重调度 + 最少使用轮询
- 失败计数 + 自动禁用 + 降级
- 图片支持（vision 模型）
- 重置失败计数

### Phase 4: 完善

- 系统设置页面
- 日志自动清理（按保留天数）
- 导入导出 CSV
- 错误处理优化
- README + 部署文档
